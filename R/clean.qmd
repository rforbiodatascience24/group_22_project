---
title: "clean"
format: html
editor: visual
---

```{r}
colnames(data)

cleaned_data <- separate(
  data = data,
  col = NAME,
  into = c("name", "BP", "MF", "systematic_name", "number"), 
  sep = "\\|\\|")
  
cleaned_data[1:3]
```
